import { StyleSheet } from '@react-pdf/renderer'
import { paddings } from '../common/common.styles';

export const styles = StyleSheet.create({
    // page: { width: '100%', padding: `${paddings.page} ${paddings.page} 0 ${paddings.page}` }
});