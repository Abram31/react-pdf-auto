import { StyleSheet } from '@react-pdf/renderer'

export const styles = StyleSheet.create({
    wrapper: { display: 'flex', flexDirection: 'row', columnGap: '10', width: '50vw', justifyContent: 'space-between', flexWrap: 'wrap' }

});