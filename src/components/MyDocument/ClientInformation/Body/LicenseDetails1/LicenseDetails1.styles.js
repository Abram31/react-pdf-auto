import { StyleSheet } from '@react-pdf/renderer'

export const styles = StyleSheet.create({
    wrapper: { display: 'flex', flexDirection: 'column', justifyContent: 'flex-start', gap: '6px', width: '100%' },
    container: { display: "flex", flexDirection: 'row', justifyContent: 'space-between', gap: '20px', width: '100%' },
    column: { display: 'flex', flexDirection: 'column', justifyContent: 'flex-start', gap: '6px', width: '100%' }
    // aboutCar: { display: 'flex', flexDirection: 'column', width: '100%', gap: '5px' }
});